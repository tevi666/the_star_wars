import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color.fromARGB(255, 213, 208, 208);
  static const Color lightBlack = Color.fromARGB(255, 75, 74, 74);
  static const Color selectedIconAndLabel = Color.fromARGB(255, 255, 255, 255);

  static const BorderSide cPrimary = BorderSide(color: primary);

  static const Color white = Colors.white;
  static const Color red = Colors.red;
  static const Color green = Colors.green;
  static const Color grey = Colors.grey;
}
