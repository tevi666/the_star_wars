import 'package:flutter/material.dart';

class AppIconThemeData {
  AppIconThemeData._();

  static iconTd(Color color, double size) => IconThemeData(
        color: color,
        size: size,
      );
}
