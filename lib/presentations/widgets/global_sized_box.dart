import 'package:flutter/material.dart';

class GlobalSizedBox extends StatelessWidget {
  const GlobalSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
